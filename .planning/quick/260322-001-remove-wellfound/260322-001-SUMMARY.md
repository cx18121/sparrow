---
phase: quick
plan: 260322-001
subsystem: scripts
tags: [cleanup, wellfound, dependencies, poll]
dependency_graph:
  requires: []
  provides: [scripts/poll.ts without Wellfound]
  affects: [scripts/poll.ts, package.json]
tech_stack:
  added: []
  patterns: []
key_files:
  created: []
  modified:
    - scripts/poll.ts
  deleted:
    - scripts/ingest-wellfound.ts
decisions:
  - Removed playwright, playwright-extra, and puppeteer-extra-plugin-stealth from package.json since these were exclusively used by the deleted Wellfound script
metrics:
  duration: "~6 minutes"
  completed: "2026-03-22"
  tasks_completed: 2
  files_changed: 3
---

# Quick Task 260322-001: Remove Wellfound Code Summary

**One-liner:** Deleted ingest-wellfound.ts and removed all Wellfound imports, constants, and conditional blocks from poll.ts plus three playwright/stealth npm dependencies.

## What Was Done

The Wellfound scraping spike proved infeasible due to Cloudflare bot detection. This task removed all dead code and unnecessary install weight.

### Task 1: Delete ingest-wellfound.ts and prune dependencies

- Deleted `scripts/ingest-wellfound.ts` (153 lines — full Playwright/stealth scraper)
- Removed three entries from `package.json` dependencies:
  - `playwright: ^1.58.2`
  - `playwright-extra: ^4.3.6`
  - `puppeteer-extra-plugin-stealth: ^2.11.2`

**Commit:** `2f6b254`

### Task 2: Remove Wellfound references from poll.ts

- Removed `import { ingestWellfound } from "./ingest-wellfound.js"` (line 7)
- Removed `const SKIP_WELLFOUND = process.env.SKIP_WELLFOUND === "true"` (line 12)
- Removed Step 2 Wellfound conditional block (10 lines)
- Renumbered remaining step comments: Product Hunt→Step 2, Crunchbase→Step 3, Apollo→Step 4

**Commit:** `e27df40`

## Verification

- `grep -ri wellfound scripts/` — no matches
- `grep -ri wellfound package.json` — no matches
- `scripts/ingest-wellfound.ts` — does not exist
- `scripts/poll.ts` — imports only: dotenv/config, node:url, _lib/prisma.js, ingest-yc.js, enrich-apollo.js, ingest-producthunt.js, ingest-crunchbase.js

## Deviations from Plan

The plan specified running `npm install` to update package-lock.json. Per task constraints, `npm install` was skipped because node_modules is on the Windows filesystem via WSL and npm install fails in this environment. package.json was updated directly by removing the three dependency entries.

## Known Stubs

None.

## Self-Check: PASSED

- `scripts/ingest-wellfound.ts`: MISSING (correctly deleted)
- `scripts/poll.ts`: FOUND and verified — zero Wellfound references
- `package.json`: FOUND and verified — playwright, playwright-extra, puppeteer-extra-plugin-stealth all absent
- Commit `2f6b254`: FOUND
- Commit `e27df40`: FOUND
