---
phase: quick
plan: 260322-001
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ingest-wellfound.ts
  - scripts/poll.ts
  - package.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "ingest-wellfound.ts no longer exists in the scripts directory"
    - "poll.ts compiles without errors and contains no Wellfound references"
    - "package.json no longer lists playwright-extra or puppeteer-extra-plugin-stealth"
  artifacts:
    - path: "scripts/poll.ts"
      provides: "Poll cycle runner with Wellfound removed"
      contains: "runPollCycle"
  key_links:
    - from: "scripts/poll.ts"
      to: "scripts/ingest-yc.js, scripts/ingest-producthunt.js, scripts/ingest-crunchbase.js, scripts/enrich-apollo.js"
      via: "named imports"
      pattern: "import.*from"
---

<objective>
Remove all Wellfound-related code from the codebase.

Purpose: Wellfound scraping was a spike that proved infeasible due to Cloudflare blocking. The code is dead weight and the playwright/stealth dependencies add unnecessary install size.
Output: Deleted ingest-wellfound.ts, cleaned poll.ts, pruned package.json.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete ingest-wellfound.ts and prune dependencies</name>
  <files>scripts/ingest-wellfound.ts, package.json</files>
  <action>
    1. Delete scripts/ingest-wellfound.ts entirely.
    2. In package.json, remove the following three entries from the "dependencies" block:
       - "playwright": "^1.58.2"
       - "playwright-extra": "^4.3.6"
       - "puppeteer-extra-plugin-stealth": "^2.11.2"
       These packages are exclusively used by ingest-wellfound.ts (confirmed by grep). No other script imports them.
    3. Run `npm install` to update package-lock.json.
  </action>
  <verify>
    <automated>test ! -f scripts/ingest-wellfound.ts && node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); const bad=['playwright','playwright-extra','puppeteer-extra-plugin-stealth']; bad.forEach(k=>{ if(p.dependencies[k]) throw new Error('still present: '+k); }); console.log('OK');"</automated>
  </verify>
  <done>File deleted, three packages absent from package.json, package-lock.json updated.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Wellfound references from poll.ts</name>
  <files>scripts/poll.ts</files>
  <action>
    Edit scripts/poll.ts to remove all Wellfound references:

    1. Remove line 7: `import { ingestWellfound } from "./ingest-wellfound.js";`
    2. Remove line 12: `const SKIP_WELLFOUND = process.env.SKIP_WELLFOUND === "true";`
    3. Remove the Step 2 block (lines 30-39):
       ```
       // Step 2: Run Wellfound unless explicitly skipped.
       if (!SKIP_WELLFOUND) {
         try {
           await ingestWellfound();
         } catch (e) {
           console.error("[POLL] Wellfound failed:", (e as Error).message);
         }
       } else {
         console.log("[POLL] Skipping Wellfound (SKIP_WELLFOUND=true)");
       }
       ```
    4. Renumber the remaining step comments so they read Step 2 (Product Hunt), Step 3 (Crunchbase), Step 4 (Apollo). The logic inside each block is unchanged.

    The resulting file should import only: dotenv/config, node:url, ./_lib/prisma.js, ingest-yc.js, enrich-apollo.js, ingest-producthunt.js, ingest-crunchbase.js.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/Clubs/genai/GenAI-Internal-Project---Cold-Email-Automation && npx tsc --noEmit --allowImportingTsExtensions --moduleResolution bundler --target esnext --module esnext scripts/poll.ts 2>&1 || grep -c "wellfound\|WELLFOUND\|ingestWellfound\|SKIP_WELLFOUND" scripts/poll.ts && echo "FAIL: references remain"</automated>
  </verify>
  <done>poll.ts contains no references to wellfound, SKIP_WELLFOUND, or ingestWellfound. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `grep -ri wellfound scripts/` returns no matches
- `grep -ri wellfound package.json` returns no matches
- `npx tsx --noEmit scripts/poll.ts` exits cleanly (or dry-run import check passes)
</verification>

<success_criteria>
- scripts/ingest-wellfound.ts does not exist
- scripts/poll.ts has no Wellfound imports, constants, or conditional blocks
- package.json has no playwright, playwright-extra, or puppeteer-extra-plugin-stealth entries
- npm install completes without errors
</success_criteria>

<output>
After completion, create `.planning/quick/260322-001-remove-wellfound/260322-001-SUMMARY.md`
</output>
