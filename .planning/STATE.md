---
gsd_state_version: 1.0
milestone: v1
milestone_name: Sparrow campaign workspace
status: active
last_updated: "2026-05-13"
---

# Project State

## Source Of Truth

Use these docs for current behavior:

- `CONTEXT.md`
- `AGENTS.md`
- `CLAUDE.md`
- `docs/adr/`
- `.planning/PROJECT.md`
- `.planning/REQUIREMENTS.md`
- `.planning/ROADMAP.md`

Older files under `.planning/research/`, `.planning/phases/`, and `.scratch/` are historical unless a current doc explicitly points at them.

## Current Product

Sparrow is campaign-first. Global navigation is Home, Templates, Settings. Campaign work lives under `/campaigns/:id/*` with Overview, Leads, Drafts, Sent, and Settings.

## Current Architecture

- Frontend: Vite + React + Tailwind.
- API: Vercel Functions-compatible router, with `local-api.ts` for local Express development.
- Database: Supabase Postgres via Prisma 7.
- Auth: Supabase Auth.
- Gmail: Google OAuth + Gmail API.
- AI: host-managed Anthropic Claude key.
- Research: Exa-first, Tavily fallback.
- Contacts: Apollo search/reveal.

## Current Decisions

- No per-user Claude or Apollo keys in the product.
- Settings has three tabs: Profile, Sending, Account.
- Templates default to verbatim mode.
- Headcount filter is retired from the audience UI.
- `LeadStatus` is SAVED, EMAILED, NO_RESPONSE, DECLINED.
- Reply detection, scheduled sends, and follow-up automation are deferred.
- **No Crunchbase.** Not a planned enrichment direction now or later. Several existing docs (`docs/scraping-research.md` Parts 2/4; ingest pipeline notes) frame Crunchbase open data as the canonical "Phase 4" fix for stage staleness, missing exit data, and the curated `PREEXISTING_PUBLICS` skiplist rot — that framing is **superseded**. External company-status enrichment, if pursued, must use a different source/approach. Future sessions that arrive at "the canonical fix is Crunchbase" via the older docs should redirect to this decision.

## Active Concerns

- Accessibility hardening: labels and modal focus management. (User has explicitly de-prioritized 2026-05-12 — not a current focus.)
- Some old planning files are archived and intentionally not reliable as current implementation docs.

## Resume hints (ephemeral)

Transient session-handoff notes. Clear after the next manual refresh or push.

- **2026-05-13 session push** pulled local in sync with `origin/main` after five commits: UI normalize + colorize (`9b14e57`), three new VC ingest adapters (Redpoint `57a81a4`; Menlo + Norwest via Bingbot UA bypass `a5c6dc9`), and the Exa-based stage+location backfill script (`3d21dee`). **422 net-new companies** across the three adapters; the Exa backfill then ran twice on the verified corpus — first against null-stage (`--only stage`, 471 stage tags + 448 location tags filled), then against the rows that still had null-location (`--only location`, 2,932 more location + 2,932 region derivations). Both runs are data-only writes, not commits — the `exa-stage-tried` / `exa-stage-enriched` row tags are the audit trail.
- **Local main was in sync with `origin/main`** as of 2026-05-12 push (`e1d5d71..7e41943`, 4 commits) — `bfa79a2` next-session pickups, `960a434` STATE refresh + audit-stages.ts result, `5dfa2ff` INVESTOR_TAGS canonical-list expansion, `7e41943` chip see-more expander across 3 surfaces. The prior push (`fbb57ae..e1d5d71`, 11 commits) landed the 2026-05-11 ingest batch — Insight, Khosla, 5 Easy wins, Exa-discovery, 5 Tier-1 mediums, 9 Tier 2/3 — totalling ~3,790 new companies across 6 ingest commits.
- **Phase 3 VC-scraper work**: steps 1–10 (Part 4) + steps 11–15 (Part 5 Easy wins) + steps 16–20 (Tier-1 mediums) + steps 21–29 (Tier 2/3) + Part 6 Exa-discovery — **all 2026-05-11 survey adapter candidates shipped.**
- **`stage=null` reduction — finished 2026-05-13 via Exa.** `scripts/backfill-stage-location-exa.ts` (Exa /search with `category=news` + 5-year recency window) ran across the 613 verified null-stage rows that Tavily hadn't reached. **80% hit rate** (450/563 in the main pass, +21/50 in the earlier test sample) — 471 stage tags filled total, with 80% also pulling a location as a bonus (448 region derivations). **Verified null-stage rate is now 1.4% (142/10,343)**, down from 5.9% pre-session. **Verified C+ count grew from 2,207 → 2,343 (+136)** — 74 new Series C, 40 new Series D, 22 new Series E. The remaining 142 nulls are mostly companies that are structurally past the Series ladder — public biotechs (Compass Therapeutics, Vir, RAPT), Indian banks (Yes Bank), and acquired/exited names — so more retrieval passes will mostly re-fetch the same news pages. The right next move there is an exit/public classifier, not another search call.
- **`location=null` reduction — also finished 2026-05-13 via Exa.** Same script run with `--only location` against the 3,235 verified rows that still had null-location after the stage pass. **91% hit rate** (2,932 / 3,235), 2,932 region derivations along with it. **Verified null-location is now 3.2% (330/10,343)** down from 31.5% pre-session, and **verified null-region is 2.4%** down from 24.3%. The 303 whiffs are companies with no recent news coverage at all; the residual 330 (slightly larger because not every news blob mentioned the HQ) is the structural floor for this retrieval strategy.
- **`stage=null` reduction (user-requested 2026-05-11) — DONE 2026-05-12d.** Combined Phase A+B+B-2 dropped null-stage verified rows **from 4,367 → 2,419 (44.1% → 24.4%, -19.7pts / -1,948 rows).** Phase A (tag-based inference, 2026-05-12b) accounted for -17.5pts; Phase B (Felicis + Costanoa source-of-truth, 2026-05-12c) for -0.9pts; Phase B-2 (Greylock + Sequoia source-of-truth, 2026-05-12d) for the remaining -1.3pts.
  - Phase B adapter outcomes: **Spark** — no-op, STATE.md was wrong; probe confirmed zero stage strings on the page. **Felicis** — stage decoded from Next.js streaming chunks via chunk-id → title map; 29 of 41 stage refs captured (remaining 12 are featured-but-exit-filtered companies like Adyen IPO — actual capture ceiling, not a window-size issue per 2026-05-12 probe). **Costanoa** — stage extracted from Prismic `data.text_cells[]` preferring "COSTANOA'S Initial investment" cell; 88 of 91 ingested rows carry stage (96.7%). **Greylock** — slug → stage map built from `<div class="portfolio-modal-box" id="<slug>">` modals containing `<div class="text-box">FIRST PARTNERED <stage>`; 150 of 153 ingested rows carry stage (98%). **Sequoia** — post_id → stage map built from `<tr data-target="#company_listing-<id>">` rows in /companies/ table, joined to WP API by company.id; 26 of 299 rows now carry stage (table only surfaces ~52 featured companies of 406 total per WP, "Early" intentionally not mapped due to Seed/Series A ambiguity). **Bessemer (354 nulls), GV (316 nulls)** — investigated, no stage data on either surface (Bessemer: 9 stage hits in 1.4MB HTML, scattered article references; GV: Sanity schema lacks the field, aiCompany.rounds[] is timing only). No-op. **General Catalyst, Index Ventures, Khosla, FoundersFund** — confirmed no stage data per existing adapter comments.
  - **Crunchbase / external enrichment** still explicitly de-scoped per the "no Crunchbase" decision (line 48).
  - Phase B-2 also added robustness: Felicis dedupe is now richest-record-wins (order-insensitive against future RSC chunk reordering); pure parsing helpers (`buildFelicisStageMap`, `normalizeFelicisStage`, `stageFromTextCells`, `normalizeCostanoaStage`, `buildGreylockStageMap`, `normalizeGreylockStage`, `normalizeSequoiaStage`, `fetchSequoiaListStageMap`) are exported and unit-tested in `scripts/__tests__/phase-b-parsers.test.ts` (18 tests) so upstream HTML/JSON shape drift fails CI before deploying a silent regression.
- **Chip list "see more" — DONE.** Shipped 2026-05-11k+l across all three chip surfaces (wizard `StepFilters`, `LeadDiscoveryTab`, campaign `SettingsTab`): top-10 chips per namespace + "+ N more" / "Show less" expander, with selected hidden chips pinned to the visible set on collapse. Did NOT extract a shared `<SectorChipGroup>` — chip styles diverge across surfaces. Browser sanity-check completed 2026-05-13 by user.
- **Older follow-ups still on the list:**
  - **Step 7** — re-ran above; result captured in 2026-05-11h.
  - **Exa-discovery breadth runs** — additional topical queries at ~1 Exa credit per 18–28 net-new rows.
  - **Balderton FacetWP unlock** (low priority) — partial-coverage adapter ships only ~30 of 200 cards because FacetWP's incremental-load AJAX is gated by a per-session nonce that doesn't reconstruct from a clean POST. Playwright path would unlock the remaining ~170.
- **Exa-discovery is operational.** `scripts/ingest-exa-discovery.ts` takes `--query "..." --topic <slug> --limit N` and ingests Exa `category=company` results. Seed run hit 8 topical queries → 179 net-new companies for 8 Exa credits. **Caveat:** company-category index doesn't support `startPublishedDate` (semantic-only) and returns some public-traded mega-caps — exit filter must happen at campaign time.
- **Battery website-extraction lesson** — `google.com/chrome` and `g2.com` are now in the chrome blocklist after the first Battery run returned google.com for 171 of 172 detail pages (browser-upgrade banner appears before the actual company link). Pattern lesson: when a "first external link" rule yields near-100% dedupe, the bug is in the blocklist, not the data. Documented in `docs/scraping-research.md` Part 5 step 20.
- **Insight Partners "needs Playwright" framing was wrong.** The `/portfolio` page is a Vue shell, but Insight is WordPress underneath — `/wp-json/wp/v2/sfcompany` (paginated list) + `/wp-json/insight/v1/get-company-content?id=<id>` (custom endpoint, double-encoded JSON wrapper around per-company rendered HTML) surface everything. Recorded as a generalizable lesson in `docs/scraping-research.md` Part 4: before reaching for Playwright, probe for `/wp-json/`, `__NEXT_DATA__`, `/_next/data/...`, or a CMS GraphQL endpoint.
- **Benchmark.com is intentionally a wall page.** Root serves 2.3 KB of HTML with office addresses + a Twitter handle; every probed sub-path 404s. Documented as step 10 (skip).

## Last Manual Refresh

2026-05-05: Root context, ADRs, README, product/design docs, env example, and active planning docs updated to match current implementation.

2026-05-11: Phase 3 VC-scraper adapters shipped (Lightspeed, IVP, Coatue) + IVP skiplist externalized to `scripts/_data/skiplists.json`. `docs/scraping-research.md` Part 4 reflects status. AGENTS.md ingest-pipeline enumeration still doesn't list `lightspeed`/`ivp`/`coatue` — small follow-up.

2026-05-11b: Phase 3 step 8 shipped — Insight Partners adapter via WP REST (509 ingested of 845 portfolio entries; 289 Prior-Investment exits filtered). `docs/scraping-research.md` Part 4 reflects status; AGENTS.md adapter enumeration updated to include `lightspeed`/`ivp`/`coatue`/`insight`.

2026-05-11c: Phase 3 step 9 shipped (Khosla — 131 ingested of 132 cards, single-page Webflow static scrape) + step 10 documented (Benchmark unreachable — no public portfolio, all probed paths 404). Net-new firm survey agent in flight (output → `.scratch/vc-survey-2026-05-11.md`) to identify the next batch of adapter candidates.

2026-05-11d: 2026-05-11 VC survey shipped (Part 5 steps 11–15 — 5 Easy single-fetch adapters): Sapphire (91), ICONIQ (100), Spark (43, with inline acquisition-spec exit filter), Initialized (181, via `__NEXT_DATA__`), Costanoa (91, via public Prismic API). **506 net-new companies in this pass.** Tier-1 mediums (~2,000 more companies) and Exa-discovery script still to ship.

2026-05-11e: Part 6 Exa-discovery shipped — `scripts/ingest-exa-discovery.ts` ingests Exa `category=company` results by topical query. Seed run: 8 topics × ~24 results = **179 net-new companies in 8 Exa credits.** Includes `category` param extension to `server/lib/ai/exa-search.ts`. Constraint discovered: company-category index doesn't support `startPublishedDate`.

2026-05-11f: 5 Tier-1 medium adapters shipped (Part 5 steps 16–20): General Catalyst (576, 100% success), Summit Partners (277, 32% no-website acts as soft exit filter), General Atlantic (393, WP REST `wp/v2/investment` + `<a class="view-site">`), Index Ventures (296, 75 IPO exits filtered via `ticker-symbol` span), Battery Ventures (171, 169 status-marked exits filtered, post-fix for google.com chrome leak). **1,713 net-new companies in this pass.** Session-wide total: **3,038 new companies across 11 commits.**

2026-05-11g: 9 Tier 2/3 adapters shipped (Part 5 steps 21–29) — Mosaic (35), BoxGroup (42), Hoxton (88), Notion Capital (64), Craft (75, **first source with stage data on list page** via fs-cmsfilter), 8VC (166), TCV (91), Felicis (171, via `\"websiteUrl\":...` regex over Next.js streaming chunks), Balderton (20, partial). **752 more companies, session total now 3,790 across 12+ commits.** All 2026-05-11 survey adapter candidates shipped.

2026-05-11h: Pushed `fbb57ae..e1d5d71` (11 commits) to `origin/main` — local is now in sync. Ran `audit-stages.ts` post-merge: DB at **11,875 verified rows**, 53.3% `stage=null`. Roll-up: Seed 4,037 / Series A 717 / Series B 528 / **Series C+ 134** / Pre-Seed 78 / Series C–I 50. The Phase 1.5 lossy-mapper patches (yc/a16z/accel) are holding cleanly. Only Craft and Balderton among the new adapters expose stage on the source — every other new growth-stage adapter (Insight 391, GC 390, GA 336, Summit 263, Battery 123) lands as `stage=null` because the page surface doesn't publish it. Notable cross-source overlap on `source` attribution (Craft 75 ingested → 43 still attributed; Initialized 181 → 99; Insight 509 → 391) — this is documented `last-write-wins` per `upsertCompany`, not an anomaly.

2026-05-11i: Expanded `INVESTOR_TAGS` in `scripts/_lib/tags.ts` from 10 to 35 slugs so the wizard's investor filter chip surface includes every adapter shipped this milestone. Confirmed via `scripts/_probe-tag-accumulation.ts` that `investor:*` tags were already accumulating cleanly on cross-source upserts (Anthropic carries 13 investor tags, Ramp 10, OpenAI/Stripe 7 each) — the data path through `mergeTags` → `reconcileCompany` was already correct; the only thing missing was the canonical-list surfacing. **The `Company.source` overwrite is metadata-only — investor signal is preserved via `tags` and queryable through the wizard.** DB-wide: of 9,912 verified rows, 998 carry ≥2 investor tags (max 13).

2026-05-11k: Chip "see more" expander shipped in 2 of 3 surfaces — `CreateCampaignWizard.tsx` (StepFilters) and `LeadDiscoveryTab.tsx`. New `CHIP_VISIBLE_CAP = 10` const per file, `expandedNs: Set<string>` state, "+ N more" / "Show less" toggle button rendered when `allTags.length > CHIP_VISIBLE_CAP`. Applied generically across SECTOR_NAMESPACES, not just `investor`. tsc clean. **Not yet browser-verified.** Codex review surfaced three follow-ups (see Resume hints): SettingsTab third surface still has `.slice(0, 8)`; selected hidden chips become visually invisible after collapse (filter still applies); browser sanity-check pending. Did NOT extract the shared `<SectorChipGroup>` component STATE.md previously suggested — two callsites with diverging chip styles (count suffix differs) doesn't justify the abstraction yet.

2026-05-11l: Closed two of the three Codex follow-ups from 11k. (1) `SettingsTab.tsx` (third chip surface) now uses the same `CHIP_VISIBLE_CAP = 10` + `expandedNs` Set pattern. (2) Pin-selected behavior added across all three surfaces: when a namespace is collapsed, any selected tag past rank 10 is appended to the visible chip list (after the top-10 by count) so a checked filter never visually disappears. The "+ N more" hidden count correctly excludes pinned items. Implementation: `visibleByCap = expanded ? allTags : allTags.slice(0, CHIP_VISIBLE_CAP); extraSelected = expanded ? [] : allTags.slice(CHIP_VISIBLE_CAP).filter(t => selected.has(t.namespaced)); tags = [...visibleByCap, ...extraSelected]`. tsc clean. Browser sanity-check still pending; nothing committed.

2026-05-12: Pushed `e1d5d71..7e41943` (4 commits) to `origin/main` — local now in sync. Final commit `7e41943` bundled the wizard, LeadDiscovery, and SettingsTab chip see-more changes + STATE 11k+l together. Prior 3 local-only commits (next-session pickups, STATE refresh, INVESTOR_TAGS expansion) shipped at the same time. Browser sanity-check on the chip surfaces still pending — only `tsc -b` has run against the chip work.

2026-05-12b: Pickup #1 (stage-null reduction) — Phase A shipped. New `scripts/_lib/stage-defaults.ts` exports `defaultStageFromTags()` with 10 narrow-thesis rules (5 growth-equity → Series C+, 1 mid-stage → Series B, 4 seed-focused → Seed, 1 YC defaulting → Seed). Wired into `upsertCompany` so future ingest self-corrects; backfilled existing rows via `scripts/backfill-stage-defaults.ts`. Tests in `scripts/__tests__/stage-defaults.test.ts` (13) + new stage-inference cases in `upsert.test.ts` (4). Inferred rows tagged `signal:stage-inferred` (added to SIGNAL_TAGS canonical list, hidden from wizard chip surfaces per HIDDEN_SIGNAL_TAGS). **Backfill outcome**: 4,367 verified null-stage rows scanned, 1,733 inferred (39.7%). Null-stage verified rate dropped 44.1% → 26.6% (-17.5pts). Top rule applications: insight (446), GA (374), summit (266), pear (163), battery (145), initialized (113), hoxton (81), tcv (62), iconiq (60), boxgroup (23). YC defaulting rule didn't fire — yc adapter's `mapYCStage` already emits stage on the normal path; the rule remains as a safety net for the default-branch null tail. Stage roll-up post-backfill: Seed 4,417 / Series C+ 1,342 / Series A 717 / Series B 673 / Pre-Seed 78 / granular Series C-I 51. Phase B (re-scrape missed surfaces — Spark/Felicis/Costanoa) still pending.

2026-05-12c: Pickup #1 — Phase B shipped (re-scrape missed surfaces). **Spark**: investigated, no code change — STATE.md was wrong that cards have "Series A in 2016" labels; probe of the live HTML returned zero canonical-stage strings anywhere on the page, the adapter's "no stage data" comment is accurate. **Felicis**: streaming-chunk decoder added — single page-scan builds a chunk-id → stage-title map (4 stages: Seed/Series A/B/C, one chunk has a leading-space title normalized off), per-company back-search resolves `\"stage\":\"$<id>\"` via the map; 29 of 40 featured-company stage refs captured (11 fall outside the existing 1500-char back-window for the URL match — diminishing returns to widen given that widening risks pulling in name fields from neighboring chunks). **Costanoa**: `data.text_cells[]` parsing added — picks the cell captioned "COSTANOA'S Initial investment" (smart-quote tolerant) as the canonical entry-round stage; 88 of 91 ingested rows carry stage (96.7%). Both adapters re-ingested. Combined Phase A+B verified null-stage rate: 44.1% → 25.7% (-18.4pts / -1,824 rows). Phase B alone: -91 rows in the null bucket, but also replaced several Phase-A-inferred Series C+ stages with granular source-of-truth Series C/A/B from Costanoa (cross-source overlap; upsert's invariant of stripping `signal:stage-inferred` when source-of-truth stage arrives held cleanly). Stage roll-up post-Phase-B: Seed 4,469 / Series C+ 1,334 / Series A 750 / Series B 677 / Pre-Seed 83 / granular Series C-I 56.

2026-05-13: **Tavily stage + location backfill shipped.** New one-shot script `scripts/backfill-stage-location-tavily.ts` per-org Tavily search → regex extract (highest Series letter wins, deterministic) → Haiku fallback when regex misses on a non-empty blob. Tag scheme: every attempted row gets `tavily-tried`; rows with at least one field filled also get `tavily-enriched`; selector excludes `tavily-tried` so a re-run never re-pays for the same row. **Three chunked passes ran (--only stage prioritized; --max-calls 200/1000/1500), enriching 2,092 verified companies (1,947 stages, 1,769 locations) before Tavily quota exhaustion.** Hit rates: stage 95% via regex alone (LLM fallback rarely fires); location 50% regex + 25% LLM + 25% whiff. **Null-stage rate dropped 36.9% → 20.5%** (under the prior 24.4% Phase-A+B floor, and well under the original 44.1% baseline before the Pickup-#1 work). Null-location dropped 46.9% → 32.0%. Tavily search wrapper (`server/lib/ai/tavily-search.ts`) gained opt-in `throwOnQuota: true` that catches 402, 429, and Tavily's non-standard 432 ("usage limit") plus body-pattern match for "usage limit / rate limit" phrasing — surfaces as typed `TavilyQuotaError` so callers can abort cleanly without permanently tagging in-flight rows. **Recovery footnote**: the first 500-row pass pre-quota-detection silently tagged 358 quota-blocked rows `tavily-tried`; recovered via inline repair script (untagged rows that had `tavily-tried` without `tavily-enriched`). Lesson logged: detect quota by body text, not status code only. **Remaining work**: ~5,388 verified rows still have null-stage or null-location, ~2,435 specifically null-stage. Resume needs more Tavily credits (current key plan exhausted) OR an Exa-based variant of the same script. ~2,400 stage-null rows × $0.008 ≈ $20 to finish.

2026-05-13b: **UI design exploration — prototypes in `.scratch/ui-prototypes/` (gitignored).** Built 8 page-level prototypes (dashboard, overview, leads, contacts, drafts, sent, templates, settings) × 3 structurally-different layouts each, plus a navigation chrome prototype (3 variants of horizontal-tab vs left-sidebar vs workflow-chevron). **Outcome: user rejected all 27 layout variants. Current production layout wins.** Real ask landed as: (1) typography is fine — *do not* change Outfit + DM Sans; (2) the "empty space" complaint is really a value-contrast complaint — `surface #F5F0E8` + `panel #F8F4ED` + `warm-200` borders sit within ~3% of each other, so panels read as one continuous cream fog rather than as discrete cards. A separate `polish.html` artifact explored A/B/C/D saturation levels for KPI / activity / campaign card tints (semantic slots: leads → tan, drafts → amber, sent → blue, replies → coral, capacity → primary green) — **deleted 2026-05-13 by user**; no saturation level was selected. The 8 page-layout prototypes + navigation.html remain as reference. **Killed ideas this session**: founder dossier (`pickFitAngle` already strong); Exa `findSimilar` cascade (primitive returns review-aggregator pages and typosquats, not semantic neighbors). **Parked**: fit-angle quality judge (#13) — useful but needs eval substrate first.

2026-05-12d: Pickup #1 — Phase B-2 + parsing tests + bundle-size win + source-attribution doc. **Greylock**: slug → stage map from `<div class="portfolio-modal-box" id="<slug>">` modals' `<div class="text-box">FIRST PARTNERED <stage>` text; 150 of 153 ingested rows carry stage (98%). "Series B+" Greylock-only label normalized to "Series B" to keep audit buckets clean. **Sequoia**: post_id → stage map from `<tr data-target="#company_listing-<id>">` table rows, joined to WP API by `company.id`. WP API fetch extended to include `id` field. Listing table only surfaces 52 of 406 featured companies; "Early" label intentionally dropped because Sequoia's Early-Stage fund covers both Seed and Series A and the column doesn't disambiguate. 26 of 299 ingested Sequoia rows now carry stage. **Bessemer (354 nulls), GV (316 nulls)**: investigated, no stage data on either surface — Bessemer has 9 scattered article-text stage hits in 1.4MB HTML; GV's Sanity schema lacks the field on `company` docs and `aiCompany.rounds[]` only encodes timing not stage. No-op. **Pure parsing helpers extracted and unit-tested**: `buildFelicisStageMap`, `normalizeFelicisStage`, `stageFromTextCells`, `normalizeCostanoaStage`, `buildGreylockStageMap`, `normalizeGreylockStage`, `normalizeSequoiaStage`, `fetchSequoiaListStageMap` all exported; 18 new tests in `scripts/__tests__/phase-b-parsers.test.ts` cover regex shape, normalization, smart-quote tolerance, and tiebreak. Felicis dedupe-by-URL changed from first-seen-wins to richest-record-wins (order-insensitive against future RSC chunk reordering). **Final null-stage rate**: 24.4% (was 25.7% post-Phase-B), combined Pickup-#1 reduction is 44.1% → 24.4% / -19.7pts / -1,948 rows. **Bundle-size**: pdfjs-dist swapped from legacy/build to modern build/min — pdf.worker chunk dropped 2.3 MB → 1.2 MB (-1.1 MB raw), pdf-parser chunk 475 KB → 404 KB (-71 KB raw). Trade-off documented in resumeText.ts: modern build requires Safari ≥ 16 / Chrome ≥ 80. **Source-attribution caveat**: AGENTS.md ingest-pipeline section now explicitly documents that Company.source is last-write-wins (a row labeled `general-catalyst` may carry investor:sequoia/a16z tags from prior ingests; wizard ORs across the full tag set). Stage-coverage line in AGENTS.md updated from 75% to 74% with reference to the new STATE entries.
